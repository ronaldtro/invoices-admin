export interface Product {
    id: number,
    productName: string,
    productAmount: number,
    productPrice: number
}

export interface ProductResponse {
    id: number,
    productName: string,
    productAmount: number,
    productPrice: number
}